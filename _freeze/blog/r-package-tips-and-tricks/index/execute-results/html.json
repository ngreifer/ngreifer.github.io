{
  "hash": "d191025d234cf24b679430264c59a518",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: \"Noah's R Package Tips and Tricks\"\nauthor: \"Noah Greifer\"\ndate: \"2024-11-14\"\ncategories:\n  - R\n  - package development\ncomments:\n  giscus:\n    repo: ngreifer/ngreifer.github.io\n    repo-id: R_kgDOH8WJeg\n    category: Blog comments\n    category-id: DIC_kwDOH8WJes4CkT3N\n    mapping: pathname\n    reactions-enabled: true\n    input-position: top\n    theme: preferred_color_scheme\n    language: en\ndraft: true\n---\n\n\n\nAs you probably know, I love writing R packages. A list of the ones I've developed can be found here, and I have contributed to many others. I have been writing R packages since I first learned R, probably around 2016. All that time has given a lot of experience that I want to share with you.\n\nThis is a \"living\" list of my tips and tricks, not really in any order, that you can peruse to help you in your own R package development or to help interpret my code if you happen to be reading it. I'll organize this into \"principles\", which are guiding principles that in theory could be implemented with arbitrary R code, and \"code\", which are specific blocks of code or functions I use frequently in my packages. Feel free to use any of these without crediting me (though of course I would like to know if anything here helps you).\n\n## Principles\n\n\n## Code\n\n### `is_null()` instead of `is.null()`\n\n`NULL` is often used in R code to represent an argument that hasn't been supplied or that something is absent or not given. We often check for `NULL` values using `is.null()`, which returns `TRUE` when its argument is exactly `NULL` as `FALSE` otherwise. Unfortunately, this can miss a lot of important scenarios where something like `NULL` is supplied, but it's not exactly `NULL`; in particular, other length-0 objects, like `integer(0L)` or `list()`. To test for these, using `identical(length(.), 0L)`, which I often abbreviate as `is_null()`. I often accompany it with a function `is_not_null()`, which is just the negation of `is_null()`.\n\n\n::: {.cell}\n\n```{.r .cell-code}\nis_null <- function(x) {\n  identical(length(x), 0L)\n}\n\nis_not_null <- function(x) {\n  !is_null(x)\n}\n\nis.null(NULL)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] TRUE\n```\n\n\n:::\n\n```{.r .cell-code}\nis_null(NULL)\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] TRUE\n```\n\n\n:::\n\n```{.r .cell-code}\na <- 1:5\nis.null(which(a < 0))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] FALSE\n```\n\n\n:::\n\n```{.r .cell-code}\nis_null(which(a < 0))\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] TRUE\n```\n\n\n:::\n\n```{.r .cell-code}\nb <- list(c = \"D\")\nis.null(b[-1])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] FALSE\n```\n\n\n:::\n\n```{.r .cell-code}\nis_null(b[-1])\n```\n\n::: {.cell-output .cell-output-stdout}\n\n```\n[1] TRUE\n```\n\n\n:::\n:::\n\n\nThere is rarely a case when I want to check specifically for `NULL` instead of checking for any length-0 object.\n\n## Extracting from `...`\n\nThe `...` function in R allows you not to have to name every argument in your function, which can be good for some lesser used arguments or for keeping your function simple. The basic method to extract information from `...` is to create a list object, e.g., `args <- list(...)`. Then you can access the arguments by name, like `args$foo`. However, a problem with this is that you have to evaluate every argument, even if it is irrelevant (at least for this part of the function). An alternative is to use R's `...elt()` function, which extracts objects from `...` by index. But because a user can enter the arguments in any order and instead is likely to name them, it would be nice to extract them by name. Someday R will add this, but for now we can use `...names()`, which extracts the names of supplied arguments to `...`, and find the position of a given argument name. I put all this into a function called `...get()`. Unfortunately, because of how `...` works, I also need to pass `...` to `...get()`. Below is what this function looks like:\n\n\n::: {.cell}\n\n```{.r .cell-code}\n...get <- function(x, ...) {\n  m <- match(x, ...names(), 0L)\n\n    if (m == 0L) {\n        return(NULL)\n    }\n\n    ...elt(m)\n}\n```\n:::\n\n\nIt returns `NULL` if the named argument isn't present. Below is how I might use it in a function to save time and memory:\n\n\n::: {.cell}\n\n```{.r .cell-code}\nf_slow <- function() {\n  Sys.sleep(.25)\n}\n\n# Slow version\ng1 <- function(...) {\n  args <- list(...)\n\n  args$this_arg\n}\n\n# Fast version\ng2 <- function(...) {\n  ...get(\"this_arg\", ...)\n}\n\nmicrobenchmark(g1 = g1(this_arg = 1, f_slow()),\n               g2 = g2(this_arg = 1, f_slow()),\n               times = 5, check = \"equal\")\n```\n\n::: {.cell-output .cell-output-stderr}\n\n```\nWarning in microbenchmark(g1 = g1(this_arg = 1, f_slow()), g2 = g2(this_arg =\n1, : less accurate nanosecond times to avoid potential integer overflows\n```\n\n\n:::\n\n::: {.cell-output .cell-output-stdout}\n\n```\nUnit: microseconds\n expr        min         lq        mean     median         uq        max neval\n   g1 247269.729 247327.170 249426.2716 249465.238 251000.524 252068.697     5\n   g2      3.362      4.141    931.3232      4.674     51.332   4593.107     5\n cld\n  a \n   b\n```\n\n\n:::\n:::\n\n\nObviously this is an artificial example but I do find this helpful, especially if `...` is passed deeply into your functions and parts of it are extracted at each step. I'm not saying this is the best way to handle `...`, but it's a way I like.\n",
    "supporting": [],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": {},
    "postProcess": true
  }
}